version: '3.7'

services:
  video-recorder:
    build: .
    container_name: blueos-PME_microDOT
    network_mode: host  # Required for BlueOS
    ports:
      - "6423:6423"  # Fixed host:container port mapping
    restart: unless-stopped
    environment:
      - BLUEOS=true